"""SpectraX SRM — API / Inference Module

Unified inference function that wraps model, uncertainty, consistency,
and metrics into a single call. Used by both the Streamlit UI and
future FastAPI backend.
"""
